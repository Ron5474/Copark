// theme.ts
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#41A9AB',
      light: '#DBEEEE',
      dark: '#2b5d5e'
    },
    secondary: {
      main: '#F45A59',
    },
  }
})

export default theme

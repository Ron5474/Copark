'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#41A9AB',
      light: '#76b4ad',
      dark: '#338882'
    },
    secondary: {
      main: '#F45A59'
    }
  }
});

export default theme;